const subset = nums =>{
    const answer = [];
    solution(nums, answer, [], 0);

    function solution(nums, answer, current, index){
        if(index >= nums.length){
            return;
        }
        answer.push(current);
        for(let i=0; i<nums.length; i++){
            
        }
    }
}